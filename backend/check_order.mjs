import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const latestOrder = await prisma.order.findFirst({ orderBy: { createdAt: 'desc' } }); console.log('Latest Order ID:', latestOrder?.orderId); console.log('Payment Method:', latestOrder?.paymentMethod); console.log('Customer Email:', latestOrder?.customerEmail); const address = await prisma.address.findFirst({ orderBy: { createdAt: 'desc' } }); console.log('Latest Address Email:', address?.email); } main().catch(console.error).finally(() => prisma.$disconnect());
